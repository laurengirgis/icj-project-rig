{% extends '_templates/base.njk' %}

{% set relative_path = "../" %}

{% block page_title %}{{ head }}{% endblock %}

{% block page_description %}{{ head }} - {{ text }}{% endblock %}

{% block content %}
  <article class="container">
    <div class="row">
      <div class="col-sm-4">
          <img src="../img/{{ slug }}.jpg" alt="" class="img-fluid mb-4">
      </div>
      <div class="col-sm-8">
          <h1>{{ head }}</h1>
          <p>{{ text | safe }}</p>
      </div>
    </div>
  </article>
{% endblock %}
